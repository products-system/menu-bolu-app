export interface SwiperSlideItem {
  id: number;
  imagePath: string;
  imageAlt: string;
  title: string;
  category: string;
}
export const scrollOptions = {
  offset: -170,
  smooth: true,
  duration: 500,
};
